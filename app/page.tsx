import CityPortfolio from "@/components/scroll/CityPortfolio";

/**
 * Home: the cinematic scroll experience.
 *  - Entry: the cityscape frame expands to fill the screen.
 *  - Then content panels rotate in and stack as you scroll.
 *
 * The clean, recruiter-focused single page still lives at /minimal,
 * and the original bento homepage at /home.
 */
export default function Page() {
  return <CityPortfolio />;
}
